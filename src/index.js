import React from 'react';
import ReactDOM from 'react-dom';
import AppWapper from './App/AppWapper.jsx';

import './index.css';

// eslint-disable-next-line react/no-deprecated
ReactDOM.render(
	<React.StrictMode>
		<AppWapper />
	</React.StrictMode>,
	document.getElementById('root')
);
